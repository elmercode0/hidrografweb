# Dependências para o app Streamlit (Streamlit Community Cloud lê este arquivo).
# A biblioteca `qualigraf` é importada de src/ (ver streamlit_app.py), não precisa ser
# instalada como pacote — mas suas dependências de runtime são listadas aqui.
streamlit>=1.36
numpy>=1.26
pandas>=2.0
matplotlib>=3.7
openpyxl>=3.1
